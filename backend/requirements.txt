# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
httpx>=0.27.0          # async HTTP — used for IP geolocation lookup
user-agents>=2.2.0     # User-Agent string parsing (browser / OS / device)

# Pipecat - voice pipeline orchestration (livekit/livekit-api pulled in as transitive deps)
pipecat-ai[silero,deepgram,cartesia,anthropic,livekit]==0.0.57
openai>=1.0.0
